# Use parenthesis to define tuples -> Tuples are like a read only list, whose values cant be changed

numbers = (10, 20, 30)

# Tuple methods
print(numbers.count(2000))  # there is no 2000
print(numbers.index(30))  # index of 3 is 2

# Accessing elements in tuples
print(numbers[1])
